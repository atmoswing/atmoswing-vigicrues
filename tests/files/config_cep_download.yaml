# Exemple de fichier de configuration

pre_actions:
  - name: Transfer SFTP CEP no variables
    uses: TransferSftpIn
    active: True
    with:
      local_dir: '/tmp'
      prefix: 'CEP'
      hostname: '127.0.0.1'
      port: 4422
      username: 'foo'
      password: 'pass'
      remote_dir: 'some/dir'
      proxy_host: ''
      proxy_port: 1080
